import React from 'react';
import css from'./Loader.module.css'; // Create this CSS file

const Loader = () => {
    return (
        <div  className={css.loader}></div>
    );
};

export default Loader;